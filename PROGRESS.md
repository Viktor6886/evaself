# PROGRESS

Оперативное состояние работы. Единственный источник истины о том, где работа
остановилась. Агент читает этот файл первым и обновляет его после каждого шага.

## Состояние

```
BASELINE:      c63849f — main после PR #303
BRANCH:        claude/eva-personality-admin-panel-2t3915
STATUS:        персона, вход в панель, политика блокировки, повторное
               распознавание картинки и качество поиска — реализовано,
               проверки локально зелёные
LAST_COMMIT:   3628af9 — оживить Еву и убрать повторный пароль из панели
BLOCKER:       —
NEXT:          PR, обязательные проверки, независимое ревью и мерж
```

## Текущая порция: картинка из истории и качество поиска

4. **Повторное распознавание картинки.** В режиме одной модели
   `preprocessSingleVision` описывал все картинки истории и клал описание
   новой репликой в конец разговора. История от Letta приходит целиком,
   поэтому каждый ход выглядел для модели как новое фото. Описание теперь
   подставляется на место картинки в её собственном сообщении, а уже
   описанные изображения берутся из `VisionDescriptionCache` (память
   процесса, восстановимая).

5. **Поиск.** `web_search` теперь читает `answers`, `infoboxes`,
   `publishedDate` и `unresponsive_engines`, принимает `category`,
   `time_range` и `language`, отличает отказ движков от пустой выдачи и
   повторяет пустой языковой запрос без фильтра, тратя квоту один раз.
   В SearXNG включён `wttr.in` (категория `weather`), `default_lang: auto`.

6. **Платёж в окне объединения.** Диагностика из PR #306: оплата за
   секунду до сообщения становилась «предыдущей репликой» и не
   применялась, а платёж последним в окне съедал сообщение перед ним.
   Каждый платёж теперь проходит своим ходом, разговор — своим. Остальное
   из #306 (уведомления об окончании подписки, read-only статус) уже есть
   в `main` после #301–#303 и не переносилось.

## Текущая порция: живость Евы и вход в административную панель

1. **Персона.** В `library/persona/eva.md` добавлен раздел «Живое общение»:
   обычный разговор — не сессия у психолога, болтовня и хорошие новости
   равноправны с разбором трудного, профессиональный контур не выключается,
   а уходит вглубь и возвращается по необходимости. Тон по умолчанию сделан
   светлее и позитивнее, у Евы явно есть свои реакции, взгляд и юмор.
   Границы, кризис и запрет обесценивания не тронуты.

2. **Пароль только при входе.** Гранты на привилегированные scope выдаёт
   `AuthService.login` самой сессии на её срок; окно повторного пароля из
   панели убрано. Действия с последствиями подтверждаются прежним окном
   `askConfirm` без пароля, необратимые — с контрольным словом. `POST /sudo`
   оставлен ради панелей, закэшированных браузером, и пароля больше не
   спрашивает. Попутно закрыт дефект: `settings:write` требовали маршруты
   персоны и настроек SDK, но выдать его было нельзя — scope отсутствовал в
   списке допустимых.

   Stack smoke в CI проверял прежнюю политику: «запись секрета без sudo —
   403», «блокировка без sudo — 403», «переписка без гранта — 403». Эти
   три проверки переписаны под новую: право даёт вход, роль по-прежнему
   решает, а сам механизм прав проверяется истёкшим грантом — запись
   после него обязана отказать.

3. **Блокировка входа.** Десять ошибок подряд — минута; дальше каждая
   ошибка снова минута; сутки без ошибок возвращают полные десять попыток.
   Считают одинаково PostgreSQL (`admin_users.last_failed_at`, миграция
   `077_admin_login_attempts`) и Valkey по IP и имени. На экране входа
   показывается, сколько ждать.

Локально зелёные: build, lint, typecheck, полный набор сервиса (1149 тестов,
1141 пройден, 8 штатно пропущено), браузерные тесты панели (112 тестов),
tenant-scope, admin-route-access, down-миграции и пути документации.

## Текущая порция: уведомления и доступ Евы к подписке

Добавляются настраиваемые предупреждения за три дня и за сутки до конца
подписки, однократное durable-уведомление при исчерпании суточной, недельной
или месячной квоты сообщений и проектный навык Евы для обязательного чтения
актуального статуса через серверный read-only инструмент.

Пороги задаются `EVA_SUBSCRIPTION_WARNING_DAYS` (по умолчанию `3,1`),
уведомления о квотах — `EVA_QUOTA_EXHAUSTION_NOTIFICATIONS`. Предупреждения
локализованы, адресуются личному Telegram ID и защищены durable-ключом по
подписке/сроку или набору периодов и их началу. Суточное уведомление сохраняет
прежний ключ, поэтому обновление не повторяет уже доставленное сообщение.

Навык `subscription-status` всегда вызывает `get_subscription_status` заново,
не принимает идентификатор пользователя и запрещает модели изменять тариф,
срок и счётчики. `/balance` теперь подписывает период каждой квоты.

Локально зелёные: build, lint, typecheck, tenant-scope, env plumbing,
down-migrations, пути документации и полный набор сервиса — 1135 тестов
(1125 пройдено, 10 штатно пропущено). После последнего теста отображения
`/balance` повторно пройдены 83 целевых теста (82 + 1 штатный skip).

Pull request [#301](https://github.com/Viktor6886/evaself/pull/301) открыт,
запущены обязательные проверки и независимое `@codex review`.

По скринам реального аккаунта найден дефект месячного расхода: сервис с
локальным `TZ` создавал первый день месяца локально, затем сериализовал его
в UTC и получал последний день прошлого месяца. Сутки и неделя поэтому
уменьшались, а месяц выглядел нетронутым. Запись всех трёх периодов перенесена
в один UTC-запрос PostgreSQL; миграция `075_quota_periods_utc` исправляет view
и переносит уже накопленные строки без потери расхода. `/balance` теперь
локализует технические метрики и сортирует периоды. Read-only инструмент
статуса регистрируется независимо от rollout-флага и отдаёт Еве все квоты,
не только сообщения. Целевые build/lint и 67 тестов зелёные.

Ревью PR #302 нашло гонку updater: миграция идёт перед пересозданием старого
writer. Перед переносом данных добавлен PostgreSQL trigger, который сразу
нормализует и его поздние записи; down-миграция снимает trigger и функцию.

## Текущая порция: pre-checkout Stars и новый период ручного доступа

`BOT_PRECHECKOUT_FAILED` возникал до списания: user-scope webhook знал
Telegram ID, но после канонической выборки пользователя не связывал с областью
внутренний `users.id`. После исправления pre-checkout обнаружился симметричный
дефект в `successful_payment`: косвенный JOIN с `users` не доказывал tenant
guard принадлежность `payment_intents`. Оба этапа теперь сначала канонически
находят и связывают оба ID, затем прямо ограничивают платёж внутренним ID.

Ручное назначение и первая платная подписка начинают собственный период квот:
исчерпанные бесплатные счётчики больше не блокируют уже выданный Plus/Pro/Max.
Платный апгрейд сохраняет расход, потому что его срок и лимиты складываются с
предыдущим тарифом. Добавлены регрессионные тесты с настоящим tenant guard и
проверкой порядка сброса квот. Отдельный тест проводит уже состоявшийся платёж
через настоящий tenant guard до записи подписки.

Первое независимое ревью указало, что общий helper ручной подписки сбрасывал
квоту и при смене плана в том же сроке. Сброс ограничен только новым
назначением; смена плана сохраняет расход и защищена отдельным тестом.

Локально зелёные: build, lint, typecheck, tenant-scope, env plumbing,
down migrations, пути документации и полный набор сервиса — 1131 тест
(1121 пройден, 10 штатно пропущено).

Pull request [#299](https://github.com/Viktor6886/evaself/pull/299): все 11
проверок CI зелёные, повторное независимое ревью не нашло замечаний, изменения
смержены в `main` коммитом `04f2fcc`.

## Предыдущая порция: составные квоты и только Telegram Stars

После проверки реального аккаунта исправлена проверка составной квоты:
дневной, недельный и месячный периоды применяются одновременно, поэтому
остаток одного периода больше не скрывает исчерпание другого. Тот же
fail-closed принцип действует для сообщений, голоса и веб-поиска.

Lava выведена из runtime, конфигурации, секретов и маршрутизации;
единственный платёжный канал — Telegram Stars. Исторические строки оплаты
и старые миграции сохраняются, чтобы обновление не повреждало аудит.

Проверки: build, lint, typecheck, tenant-scope, env plumbing, down migrations,
маршруты, 112 браузерных тестов панели и полный набор из 1127 тестов сервиса.
Pull request [#296](https://github.com/Viktor6886/evaself/pull/296): все
обязательные проверки зелёные, повторное независимое ревью без замечаний,
смержен в `main`.

## Предыдущая порция: жизненный цикл подписки и read-only статус

Повторная покупка действующего тарифа и понижение блокируются до списания,
одновременно возможен только один Stars checkout. Повышение Plus → Max
сохраняет оплаченный остаток: срок складывается, периодические квоты
взвешиваются по оставшимся и новым дням, total-квоты суммируются. Фактические
лимиты конкретной подписки хранит миграция `074_subscription_lifecycle`.

Ева получила один серверный read-only инструмент без аргумента пользователя:
тариф, срок, оставшиеся дни, текущие квоты и бесплатные сообщения. Отдельный
детерминированный сервис без LLM ставит в durable outbox предупреждение за
24 часа до конца, один раз для каждой пары «подписка + срок».

Проверки: targeted payment/subscription tests, build, lint, typecheck,
tenant-scope и полный набор тестов сервиса. Pull request
[#295](https://github.com/Viktor6886/evaself/pull/295) открыт, CI и независимое
ревью ожидаются.

Независимое ревью указало на fail-safe квот, пагинацию уведомлений и
отсутствовавший rollout-флаг. Исправления добавлены; новый жизненный цикл
включается `EVA_SUBSCRIPTION_LIFECYCLE=false → true` после canary.
Повторное ревью дополнительно выявило смешивание конечной квоты с безлимитом
и риск отправки предупреждения в групповой чат: безлимит теперь включается
после старого срока, а уведомление всегда адресуется личному Telegram ID.
Финальное ревью добавило случай повторного платежа: уже купленная дата
начала безлимита сохраняется и не отодвигается новым начислением.
Подтверждение платежа сообщает фактически сохранённый fail-safe тариф.
Бессрочный доступ защищён отдельно: ограниченная покупка не переносит
бессрочность на новый, более высокий тариф, в том числе при rollback-флаге.

## Предыдущая порция: надёжная активация подписки после оплаты звёздами

Telegram подтверждал списание, но ошибка `stars.apply()` проглатывалась в
workflow. Durable update после этого получал ложный `completed`, поэтому ни
подписка, ни повторная попытка не появлялись. Платёж вынесен перед Letta и
прочими зависимостями диалога, ошибки применения теперь доходят до retry
inbox. При старте старые успешные платежи без записи в `payments`
возвращаются в очередь; повтор безопасен по `telegram_payment_charge_id`.

Проверки: build, lint, typecheck, tenant-scope и полный набор тестов сервиса.
Pull request [#293](https://github.com/Viktor6886/evaself/pull/293): все
обязательные проверки зелёные, независимое Codex-ревью без замечаний, смержен.

## Предыдущая порция: единая административная панель

Административная часть сведена в одну панель на `https://<домен>/admin`.
Публичных имён осталось три: корневой домен, `app.` и `api.` Консоль Letta
и страница статуса стали разделами панели, их поддомены отвечают только
308-редиректом, а сервисы `letta-ui` и `uptime-kuma` выведены из
эксплуатации.

Новое в панели: агенты (через production-путь Letta Agent SDK), подписки
(ручное решение отличимо от оплаты), персона и системный промпт (правка
настоящих production-источников с немедленным применением), Letta и
мониторинг.

Миграция `067_admin_panel_unification`. Проверки:
`scripts/ci/assert-single-admin-domain.py` и
`scripts/ci/test-admin-routing.sh`.

Pull request #268, обязательные проверки зелёные (прогон 648). Мерж ждёт
независимого ревью: протокол его требует, а в сессии реализации его
выполнить нельзя — автор изменения не оценивает собственную готовность.

Значения полей:

- `BASELINE` — коммит `main`, от которого отсчитывается текущее состояние.
- `BRANCH` — рабочая ветка текущей порции, `—` пока работа не начата.
- `STATUS` — `нет активной работы` · `в работе` · `на ревью` · `заблокирован` ·
  `выполнен`.
- `LAST_COMMIT` — последний checkpoint-коммит в `BRANCH`. Точка возобновления
  после обрыва сессии: код берётся отсюда, а не из памяти сессии.
- `BLOCKER` — причина остановки либо `—`. Непустое значение запрещает продолжать.
- `NEXT` — одно конкретное следующее действие, а не план.

Блок обновляется до пуша: если сессия оборвётся, следующий агент восстанавливает
работу из Git и этого блока.

## Точка отсчёта

Архитектура приведена к Letta-native: Letta Agent SDK и App Server —
единственный когнитивный runtime, Evaself — продукт вокруг него. Самописный
cognitive middleware удалён целиком (PR #188). Граница ответственности —
`CLAUDE.md`, раздел «Главная граница», и `docs/letta-native.md`.

Что это значит для планирования: **прежний roadmap по шагам 27–67 больше не
является обязательной работой.** Он строился вокруг подсистем, которых в
репозитории нет — графовой и temporal памяти, Memory Curator, Hybrid
Retrieval, SkillRouter, ToolGateway. Возобновлять его как есть нельзя: это
означало бы восстановить удалённый cognitive middleware в обход инварианта 3.

Архив прежнего roadmap со статусами выполненных batch — `docs/ROADMAP_ARCHIVE.md`.
Он читается только как история: что уже сделано и зачем. Заданием он не является.

## Что вошло в базовую точку

Восемь шагов приёмки поверх `f104d90`, по одному коммиту на шаг:
источник истины архитектуры (1), разрешения, публичный SDK и приватность
трассы (2), роутер без семантической классификации (3), контекст хода без
prompt middleware (4), готовность по фактам runtime и smoke памяти (5),
capability probe модели (6), необязательная инфраструктура вне обязательного
пути (7), финальная приёмка: сверка ссылок, мёртвый код, документация (8).

Поверх них — доработки по результатам аудита и первого прогона CI: путь
исследования (conversation открывает сервер, отказы получили устойчивый
JSON-контракт), готовность со сроком годности и состоянием
`ready`/`degraded`/`not_ready`, кризисное событие без прямой речи,
отдельный ключ шифрования конфигураций, потолок и метрики служебного
блока, живой smoke отдельным workflow. Красные проверки CI разобраны по
первопричинам: клиент PostgreSQL по версии сервера, fail-closed сравнение
схем, смысловые инварианты вместо числа строк, порядок проверки
готовности.

## Как берётся следующая работа

Работу задаёт человек. Порция работы = одна ветка, один pull request, один
прогон CI, одно ревью — протокол в `CLAUDE.md`, раздел «Batch-протокол».

Прежде чем взять задание из `prompts/step-NN-*.md`, сверь его с разделом
«Главная граница» в `CLAUDE.md`. Шаг, который строит собственную память,
собственное извлечение, свой роутер навыков или подходов, свой compaction или
свою ротацию conversation, выполнять нельзя — сначала это обсуждается с
человеком.

Шаг получает статус `выполнен` только внутри смерженной порции. Порция получает
статус `выполнен` только после зелёных обязательных проверок на pull request,
независимого ревью и мержа.

Красная проверка, упавший тест и недоступный перезапуск job статус
`заблокирован` не дают: они чинятся по разделу «Красная проверка» в
`CLAUDE.md`. `заблокирован` ставится только при неустранимой внешней
зависимости — раздел «Остановка» там же.
