# OSINT: исследование открытых источников

Ева умеет исследовать открытые источники по людям, аккаунтам, организациям,
телефонам, почте, доменам, IP-адресам, ASN, публичным документам и
официальным реестрам. Этот документ — архитектура контура, его границы и
порядок ввода.

Статус: batch OSINT-1…6 выполнены — сборщики профилей, веба,
инфраструктуры и реестров РФ, задания, инструменты Евы, отчёт и Mini App.
Контур выключен флагами, пока его не включит человек. Порядок ввода — в
конце документа.

## Главная граница

Letta остаётся единственным когнитивным runtime Евы. OSINT — не второй
агент, а набор детерминированных компонентов, которыми Ева управляет через
несколько высокоуровневых инструментов:

| Решает Ева (Letta) | Решает код OSINT-контура |
|---|---|
| когда начать исследование и что спросить у человека | нормализацию идентификаторов |
| как объяснить отчёт человеку | выбор сборщиков и порядок их запуска |
| предложить план поиска и кандидатов на совпадение | проверку и урезание плана по бюджету |
| | тождество сущностей по признакам и порогам |
| | уверенность утверждений и их статус |
| | поиск противоречий |

Модель может предложить, что две записи описывают одного человека, но
объединяет их только детерминированный код (`src/osint/resolver.ts`) по
признакам с доказательствами. Совпадения имени для этого недостаточно
никогда.

Данные OSINT — о третьих лицах, а не память Евы. Они живут в таблицах
`osint_*` и не попадают в memory blocks Letta (`human`, `persona`,
`current_state`, `therapeutic_framework`). Evaself их туда не пишет.

## Кому доступно и на каких условиях

Решение владельца (2026-09-25): исследование людей доступно всем
пользователям. Это исключение из инварианта 30 корневого `CLAUDE.md`
(«скрытый анализ третьего лица запрещён»), и оно действует только на
следующих условиях:

- исследование начинается только по явной просьбе человека, не по
  инициативе Евы;
- у исследования есть заявленная цель (`osint_investigations.purpose`,
  `NOT NULL`): без неё запись не создаётся;
- у каждого пользователя лимит исследований и бюджет каждого из них;
- создание, просмотр и удаление исследования пишутся в аудит;
- контур выключен флагом по умолчанию и включается человеком.

Запрещено независимо от флагов:

- утёкшие базы, дампы учётных данных, украденные базы, боты «пробива»,
  базы операторов связи и банков, закрытые государственные базы;
- обход авторизации и CAPTCHA, подбор паролей, взлом аккаунтов;
- распознавание лиц для идентификации человека;
- отслеживание точного физического местонахождения человека;
- связь человека с IP-адресом без публичного источника, прямо её
  утверждающего;
- проверка существования аккаунтов перебором (Holehe и подобные) в
  обычном режиме.

## Модель данных

### Идентификатор и сущность — разные вещи

`ivan@example.com` — идентификатор: строка, которую можно нормализовать и
искать. Человек, которому адрес принадлежит, — сущность. Связь между ними
(`osint_entity_identifiers`) сама требует доказательства, источника и
уверенности.

Виды идентификаторов (`src/osint/types.ts`): `name`, `username`, `email`,
`phone`, `url`, `domain`, `ip`, `cidr`, `asn`, `organization`, `tax_id`,
`registration_number`, `social_account`, `document`.

### FollowTheMoney

Семантика сущностей и связей — FollowTheMoney 4.11.0 (MIT, OpenSanctions),
чтобы граф выгружался в Aleph, yente и nomenklatura без перевода:

- сущности: `Person`, `Organization`, `Company`, `LegalEntity`,
  `PublicBody`, `UserAccount`, `Address`, `Document`;
- связи: `Ownership`, `Directorship`, `Membership`, `Employment`, `Family`,
  `Associate`, `Representation`, `UnknownLink`;
- свойства утверждений — имена свойств FtM: `birthDate`, `innCode`,
  `ogrnCode`, `email`, `phone`, `username`.

Где FtM не покрывает задачу, модель расширена, и расширение видно по
имени:

- **инфраструктура** — `eva:Website`, `eva:Domain`, `eva:IPAddress`,
  `eva:Network`. В FtM 4.11.0 домен, IP и ASN — значения свойств, а не
  сущности (проверено по списку схем пакета);
- **почта и телефон остаются идентификаторами**, а не сущностями
  `Email`/`PhoneNumber`. В FtM схема `Email` — это письмо, а не адрес, и
  назвать так адрес значило бы сломать совместимость, ради которой FtM
  выбран.

FollowTheMoney и nomenklatura — библиотеки Python, а `eva-agent-service`
написан на TypeScript. Поэтому они работают в `osint-worker` (batch
OSINT-2). В TypeScript хранится совместимая схема и политика решений.

### Таблицы (`postgres/migrations/084_osint_foundation.sql`)

| Таблица | Что хранит |
|---|---|
| `osint_investigations` | запрос, цель, режим, статус, бюджет, ключ идемпотентности |
| `osint_entities` | сущность пользователя: схема и подпись |
| `osint_investigation_entities` | какие исследования используют сущность |
| `osint_identifiers` | нормализованные идентификаторы, уникальные у пользователя |
| `osint_entity_identifiers` | связь сущности с идентификатором — с доказательством |
| `osint_sources` | источник: адрес или локатор API, сборщик, уровень, качество |
| `osint_evidence` | цитата или структурированный фрагмент и его отпечаток |
| `osint_claims` | утверждение о свойстве сущности со статусом и уверенностью |
| `osint_relationships` | связь между сущностями по схеме FtM |
| `osint_entity_matches` | решение о тождестве: статус, счёт, признаки |
| `osint_collector_runs` | прогон сборщика и его исход, включая деградацию |
| `osint_frontier` | очередь итеративного расширения с глубиной |

Граница арендатора держится в самой схеме. В каждой таблице есть
`user_id NOT NULL`, и каждая ссылка между таблицами идёт по паре
`(id, user_id)`: утверждение одного пользователя не может сослаться на
сущность или доказательство другого, даже по известному UUID. Это
проверяет `scripts/ci/test-osint-schema.sql` на настоящем PostgreSQL. Все
таблицы внесены в реестр `src/tenancy/tables.ts`.

Удаление исследования каскадно уносит его источники, доказательства,
утверждения, связи и прогоны. Сущности пользователя остаются: их могут
использовать другие его исследования. Сущности, которые не использует ни
одно исследование, будет чистить сервис хранения (batch OSINT-3).

## Доказательства и уверенность

Ни одно утверждение не становится фактом только потому, что так сказал
сборщик или модель. У каждого утверждения есть сущность, свойство и
значение, источник (адрес или локатор, сборщик, время), доказательство и
его отпечаток SHA-256, уверенность и статус.

Доказательство (`src/osint/evidence.ts`) бывает двух видов:

- **цитата** — обязана буквально входить в текст источника. Проверка та
  же, что в `ResearchOrchestrator`: цитата, которой нет в тексте, — догадка
  модели;
- **структурированный фрагмент** ответа API. Отпечаток считается по
  каноническому JSON, и порядок ключей его не меняет.

Уверенность (`src/osint/confidence.ts`) считает код:

- у источника есть уровень, от официального реестра (0.95) до
  неизвестного сайта (0.2);
- независимыми считаются только разные домены: перепечатка одной статьи
  двадцатью агрегаторами — одно свидетельство;
- «подтверждено» требует первичного источника: официального реестра,
  сайта, профиля или первичного документа;
- слабые источники в любом количестве не поднимаются выше потолка своего
  уровня;
- противоречие перекрывает любую уверенность.

## Тождество сущностей

`src/osint/resolver.ts` выносит решение `confirmed`, `probable`,
`possible`, `conflicting` или `rejected` по признакам. Каждый признак
обязан ссылаться на доказательства; признак без доказательства в решение
не входит.

| Признаки | Вес |
|---|---|
| одинаковый ИНН или ОГРН | тождество |
| профили ссылаются друг на друга | 0.9 |
| один профиль ссылается на другой | 0.6 |
| общий подтверждённый e-mail или телефон | 0.7 |
| общий публичный сайт | 0.5 |
| одинаковая дата рождения | 0.3 |
| точное совпадение username | 0.25 |
| имя, город, работодатель, должность, биография, общие связи | 0.05–0.15 |

Признаки против:

- **жёсткие** — разный ИНН или ОГРН, разная дата рождения, несовместимый
  возраст;
- **мягкие** — разная страна, противоречащие организации, разные биографии.

Правила, которые важнее весов:

- без сильного связующего признака (ссылки между профилями, общей почты
  или телефона) — не выше `possible`: общий сайт и одинаковый username
  таким признаком не считаются, коллеги-тёзки ссылаются на один сайт
  компании;
- `confirmed` требует счёта не ниже 0.9 и двух независимых сильных
  признаков либо взаимных ссылок профилей;
- жёсткое «против» при сильном «за» — `conflicting`, без сильного «за» —
  `rejected`;
- сравнение фотографий как биометрия не является признаком ни с каким
  весом.

Пороги и веса меняются только вместе с тестами `test/osint-core.test.ts`.

## Противоречия

`src/osint/contradictions.ts` ищет разногласия в свойствах с единственным
значением: дата и место рождения, пол, ИНН, ОГРН, КПП, юрисдикция.
Разногласие не разрешается выбором «лучшего» источника: оно сохраняется
и показывается в отчёте. Даты разной точности согласуются: «1990» не
спорит с «1990-05-01». У свойств с многими значениями (почта, адрес,
работодатель) второе значение — дополнение, а не спор.

## Приватность журналов

OSINT почти целиком состоит из персональных данных третьих лиц.

- В журнал и метрики идёт маска (`i***@e***.com`, `+7***89`) или псевдоним:
  HMAC с секретом установки (`src/osint/masking.ts`).
- В маске всегда три звёздочки, чтобы по ней не угадывалась длина значения.
- Сами значения в телеметрию не отправляются.

## Выбор компонентов

| Компонент | Лицензия | Роль | Batch |
|---|---|---|---|
| libphonenumber-js 1.13.14 | MIT (проверено) | телефоны в E.164 | 1 |
| FollowTheMoney 4.11.0 | MIT (проверено) | модель сущностей | 2 |
| nomenklatura 4.17.0 | MIT (проверено) | признаки сходства `LogicV2` для тождества | 2 |
| Maigret 0.6.6 | MIT (проверено) | username → профили | 2 |
| WhatsMyName, данные `062bcfe` | CC BY-SA 4.0 (проверено) | независимая проверка профиля | 2 |
| Sherlock, данные `3760187` | MIT (проверено) | независимая проверка профиля | 2 |
| SpiderFoot, master `0f815a2` | MIT (проверено; релиз 4.0 — GPL-2.0) | пассивный DNS, реестры, репутация | 4 |
| theHarvester 4.11.1 | GPL-2.0-only (проверено) — отдельный контейнер | домен → хосты, почта, адреса | 4 |
| dnspython 2.8.0 | ISC (проверено) | записи DNS | 4 |
| RDAP (IANA bootstrap), RIPEstat, crt.sh | открытые API | инфраструктура | 4 |
| Поиск ЕГРЮЛ/ЕГРИП ФНС (egrul.nalog.ru) | открытые сведения (129-ФЗ, ст. 6) | организации и ИП по ИНН, ОГРН, наименованию | 6 |
| yente | отложен | fuzzy matching | позже |

yente отложен по решению владельца: ему нужен Elasticsearch/OpenSearch
(+2–4 ГБ памяти), а данные OpenSanctions, с которыми его обычно используют,
для коммерческого продукта, по нашему пониманию, требуют отдельной
лицензии; это проверяется перед подключением. В первом релизе тождество
решают nomenklatura в `osint-worker` и `resolver.ts`.

Инструменты с лицензиями GPL и AGPL подключаются только отдельным
сервисом по HTTP, без копирования их кода в Evaself.

## osint-worker (batch OSINT-2)

Отдельный Python-сервис `osint-worker/` в сети `tools`, без портов,
включается профилем compose `osint` и по умолчанию не собирается и не
запускается. Каждый запрос предъявляет `OSINT_WORKER_TOKEN`
(`X-Osint-Key`); без токена при `EVA_ENV=production` сервис не стартует.
Корень контейнера только для чтения, capabilities сброшены.

- **Maigret** вызывается как библиотека: база сайтов из пакета, без
  автообновления, без обхода Cloudflare, прокси и Tor; число сайтов,
  время на сайт и общий срок ограничены.
- **WhatsMyName и Sherlock** — только наборы правил, закреплённые по
  коммиту и sha256 (`osint-worker/tools/fetch_datasets.py`). Проверку
  выполняет свой код через `netguard.py`: каждый адрес и каждый редирект
  проверяются — только http/https, порты 80/443, только публичные адреса.
  Сайты с пометкой `protection` пропускаются.
- **Пределы нагрузки**: не больше двух запросов к домену одновременно;
  после трёх отказов подряд (429, капча, таймаут) домен пропускается пять
  минут. Капча и лимит — деградация сборщика, а не «аккаунта нет».
- **Исключённые категории**: знакомства, здоровье и медицина, религия,
  политика, взрослый контент, геосоциальные сервисы. Само наличие
  аккаунта там раскрывает особые категории данных или местоположение.
- **nomenklatura** возвращает признаки, а не решение. `worker-mapping.ts`
  переводит их в признаки `resolver.ts` по фиксированным порогам
  (`name_match` ≥ 0,95 → `name_exact`, ≥ 0,7 → `name_partial`, совпавший
  ИНН/ОГРН, разные даты рождения, разные страны). Итог выносит resolver.
- **Сведение сборщиков**: находка Maigret — одно подтверждение;
  «аккаунт найден» у WhatsMyName или Sherlock добавляет подтверждение,
  «не найден» — противоречие, «не смог проверить» — отдельная отметка.

Клиент `src/osint/worker-client.ts` в runtime пока не подключён: его
вызывает оркестратор OSINT-3. Скан не повторяется при сбое (сотни
запросов к чужим сайтам), чтения повторяются один раз.

Лицензии зависимостей воркера: всё MIT/BSD/Apache, кроме `pycountry` и
`python-stdnum` (LGPL, используются без изменений как библиотеки в
отдельном сервисе). Данные WhatsMyName распространяются под CC BY-SA 4.0:
атрибуция — в `osint-worker/README.md`. Образ — около 1 ГБ (scipy,
scikit-learn, duckdb тянет nomenklatura).

## Исследование: оркестратор, задания, хранение (batch OSINT-3)

Исследование создаёт `OsintService` (`src/osint/service.ts`). Условия
проверяются в сервисе, а не у вызывающего:

- флаг `EVA_OSINT_ENABLED` (по умолчанию `false`) — выключенный флаг
  не пускает запрос даже до базы;
- заявленная цель (3–1000 знаков) и от 1 до 10 идентификаторов, каждый
  нормализован;
- дневной лимит `EVA_OSINT_DAILY_LIMIT` (по умолчанию 3), считается под
  блокировкой строки пользователя;
- ключ идемпотентности: повтор возвращает то же исследование;
- аудит в `audit_log`: id, режим, типы идентификаторов — без запроса,
  цели и значений.

Идентификаторы запроса становятся идентификаторами субъекта
(доказательство — сам запрос, источник `seed`) и встают в очередь
`osint_frontier` на глубине 0. Работа идёт заданием `osint_investigation`
в очереди `research` (job outbox → BullMQ); в конверте задания только id.

`OsintOrchestrator` (`src/osint/orchestrator.ts`) берёт элементы очереди по
глубине и запускает подходящие сборщики:

| Сборщик | Типы | Что делает |
|---|---|---|
| `maigret` | username | скан Maigret и проверка найденного WhatsMyName/Sherlock через osint-worker |
| `web_search` | имя, username, почта, телефон, организация, домен, ИНН, ОГРН | SearXNG → Crawl4AI; источник — только страница, где идентификатор есть буквально, доказательство — цитата из неё |

Сборщики детерминированные, модели нет ни в выборе запросов, ни в
чтении. Поэтому веб-сборщик переиспользует адаптеры `ResearchOrchestrator`
(`SearxCrawlAdapters`), но не сам оркестратор разбора темы: тот
планирует запросы и извлекает факты моделью.

Расширение ограничено: по имени и названию организации найденные на
странице профили в очередь не идут (профиль тёзки — не след человека);
идут профили со страниц, где буквально названы искомые почта, телефон или
username, и username-ы, которые Maigret извлёк из найденного профиля.
Бюджет (`InvestigationBudget`: глубина, идентификаторы, сущности, внешние
запросы, 15 минут) проверяется перед каждым шагом; сборщик получает
остаток и не тратит больше.

Найденный аккаунт — отдельная сущность `UserAccount`; свойства профиля —
утверждения о нём, а не о субъекте. Связь с субъектом выносит resolver;
совпадение одного ника для него — «данных мало», и такое решение не
записывается вовсе, а не записывается ложным «отвергнуто».

Повтор задания после сбоя продолжает с того же места: завершённый прогон
сборщика по той же цели не повторяется (уникальный индекс прогонов),
источники и доказательства не дублируются. Отмена (`cancel`) действует на
границе шага; удаление (`delete`) уносит граф исследования и сущности,
не нужные другим исследованиям.

Хранение: класс `osint_investigations` в политиках хранения, по умолчанию
90 дней (`retention.osint_investigations_days`, 7–365). Удаляются только
завершённые исследования, затем осиротевшие сущности и идентификаторы.

Инструменты Letta и API Mini App — раздел «Инструменты, отчёт, Mini App»
(OSINT-5).

## Инфраструктура (batch OSINT-4)

Три новых сборщика, все без модели и без ключей:

| Сборщик | Типы | Источник | Что даёт |
|---|---|---|---|
| `infrastructure` | домен, IP, AS | osint-worker: RDAP по IANA bootstrap, RIPEstat, crt.sh, DNS | сущности `eva:Domain`, `eva:IPAddress`, `eva:Network` со свойствами из реестров |
| `theharvester` | домен | osint-harvester | хосты, адреса, почта, AS домена |
| `spiderfoot` | домен, IP | osint-spiderfoot | пассивный DNS, реестры, репутационные списки |

Ни один источник не обращается к исследуемому хосту: реестры, журналы
сертификатов, пассивный DNS и резолвер.

**Приватность.** Из RDAP берутся только организации: физическое лицо
(vCard `kind: individual`) и скрытые регистратором записи не извлекаются.
SpiderFoot отдаёт наружу только инфраструктурные события; имена людей,
физические адреса, геоданные и сырые ответы реестров отбрасываются в
контейнере. Модули и источники утечек (psbdmp, scylla, wikileaks,
haveibeenpwned, dehashed, leakix, intelx, hudsonrock) запрещены явно,
`sfp_whois`, `sfp_pgp` и `sfp_keybase` не включены: они приносят контакты
людей.

**Почта организации.** theHarvester возвращает только адреса, уже
опубликованные в открытых источниках и принадлежащие исследуемому домену
(`info@`, `press@`, адреса сотрудников на сайте компании). Это данные
организации, а не контакты частного лица из закрытых баз: ни базы утечек,
ни сервисы поиска людей не используются. Адрес идёт в веб-поиск в пределах
бюджета (не больше 10 за прогон) и хранится в графе исследования со
сроком хранения исследования; в memory blocks он не попадает.

**Расширение.** Поддомены из журналов сертификатов, пассивного DNS и
theHarvester запоминаются (привязаны к домену), но в очередь не встают:
их сотни, и каждый не стоит отдельного шага. Публичные адреса, AS и почта
встают — не больше 10 каждого вида за прогон. Адреса из частных и
служебных диапазонов в очередь не попадают вовсе. Организация-регистрант
идёт в веб-поиск.

**Лицензии.** theHarvester — GPL-2.0-only: отдельный контейнер, обёртка
`osint-harvester/app/main.py` под той же лицензией, Evaself обращается к
нему только по HTTP. SpiderFoot взят из ветки master (MIT) по коммиту:
релиз 4.0 выходил под GPL-2.0 и тянет `cryptography<4`. Пины upstream у
обоих содержат версии с известными уязвимостями (aiohttp, ujson,
cryptography, pyOpenSSL) — зависимости ставятся из своих lock-файлов с
исправленными версиями, pip-audit проверяет их в CI. Стадия `test` каждого
образа сверяет, что все разрешённые источники и модули существуют в
закреплённой версии.

## Инструменты, отчёт, Mini App (batch OSINT-5)

**Инструменты Евы** (`src/osint/tools.ts`) регистрируются, только когда
включён `EVA_OSINT_ENABLED` и есть слой заданий; иначе их в наборе нет.
Когда вызывать, решает Letta (инвариант 17), условия проверяет сервис.

| Инструмент | Риск | Что делает |
|---|---|---|
| `osint_investigate` | `sensitive_write` — подтверждение человека | ставит исследование в очередь; цель, лимит и аудит проверяет сервис |
| `osint_get_status` | `read` | ход исследования |
| `osint_get_report` | `read` | отчёт текстом с ограничениями |
| `osint_list` | `read` | последние исследования пользователя |
| `osint_search_entity` | `read` | поиск по уже собранному, без внешних запросов |
| `osint_cancel` | `low_risk_write` | остановка на границе шага |
| `osint_delete` | `destructive`, `data_deletion` | удаление с подтверждением |

Владелец берётся из хода, а не из аргументов модели. Ключ идемпотентности —
хэш conversation и идентификатора вызова: повтор хода после сбоя возвращает
то же исследование. Описания инструментов говорят модели то же, что
проверяет сервис: только по явной просьбе, с целью, без «пробива», утечек,
слежки; сведения о третьих лицах из отчёта не сохраняются в память.

`osint_expand` и `osint_explain_relationship` из первоначального плана не
сделаны: расширение идёт автоматически в пределах бюджета, а связи между
сущностями (`osint_relationships`) сборщики пока не заполняют — инструмент
отвечал бы пустотой.

**Отчёт** (`src/osint/report.ts`) собирается из графа без модели: субъект,
аккаунты с источниками и статусом связи с субъектом, инфраструктура,
дословные цитаты, найденные идентификаторы, прогоны сборщиков. Раздел
ограничений есть всегда: деградировавшие источники («не смогли проверить» ≠
«ничего нет»), исчерпанный бюджет, неподтверждённая принадлежность
аккаунтов. Просмотр отчёта и поиск пишутся в аудит.

**Уведомление.** По завершении задание кладёт в `telegram_outbox` короткое
сообщение только со счётчиками (ключ `osint-done:<id>`, повтор не дублирует);
ни имён, ни адресов в нём нет.

**Mini App** (`src/public/osint-routes.ts`): `GET /public/osint`,
`GET /public/osint/:id`, `GET /public/osint/:id/report`,
`POST /public/osint/:id/cancel`, `DELETE /public/osint/:id`. Начать
исследование из Mini App нельзя: условие исключения — просьба в разговоре
и подтверждение действия.

**Метрики**: `eva_osint_investigations_total{outcome}`,
`eva_osint_collector_runs_total{collector,status}`,
`eva_osint_external_requests_total{collector}`. Метки — закрытые наборы,
без запросов и идентификаторов.

**Evals**: набор `osint` (`operation: osint_boundaries`) — без цели,
с неизвестным типом (например, «пароль из утечки») и без идентификаторов
исследование не создаётся, запуск требует подтверждения; ослабленная
граница валит набор.

## Реестры РФ (batch OSINT-6)

Сборщик `egrul` (`src/osint/registry-collectors.ts`, osint-worker
`app/registries.py`) ищет в ЕГРЮЛ и ЕГРИП через публичный сервис ФНС
`egrul.nalog.ru`. Сведения реестров открыты по закону (129-ФЗ, ст. 6).

| Запрос | Что даёт |
|---|---|
| ИНН, ОГРН/ОГРНИП (российские, с контрольной суммой) | запись организации или ИП |
| наименование организации (3–200 знаков) | до 10 организаций с похожим именем |

- Организация — сущность `Organization`, ИП — `LegalEntity`; ключ — ИНН.
  У исследования организации по её ИНН сведения реестра ложатся на сам
  субъект, и отчёт показывает их в строке «Сведения о субъекте».
- Свойства: наименование, сокращённое наименование, ИНН, ОГРН, КПП, даты
  регистрации и прекращения, статус, регион; у организации — адрес и
  руководитель. У ИП адреса нет: сервис его не показывает, и мы его не ищем.
- ФИО руководителя — свойство организации, а не новый след: человек за ним
  в поиск не отправляется. Поиск ИП по ФИО не поддерживается: однофамильцы
  не след, а шум.
- Организации из поиска по наименованию — отдельные сущности-кандидаты;
  ни одна не объявляется субъектом, их номера в очередь не встают.
- Капча — деградация `captcha`, без попыток обхода; частые запросы ФНС
  встречает именно ею, поэтому к сервису идёт один запрос за раз и не чаще
  раза в секунду, а клиент не повторяет запрос при сбое. Незнакомая форма
  ответа — деградация `unavailable`, а не «организации нет».
- Выписка (PDF) не скачивается: для отчёта хватает строки поиска.

**Контракт не сверен вживую.** Из окружения разработки сеть до
`nalog.ru` закрыта, и форма ответа сервиса взята из его публичного
поведения. Поэтому у сборщика свой флаг `EVA_OSINT_RU_REGISTRIES`
(выключен): сначала проверка на развёртывании — один запрос по известному
ИНН и сверка отчёта, — затем включение.

**Не подключены** «Прозрачный бизнес» (pb.nalog.ru), ЕФРСБ
(fedresurs.ru) и картотека арбитражных дел (kad.arbitr.ru). У них нет
открытого машинного интерфейса, контракт которого можно сверить, а
публичные страницы защищены от автоматических запросов, и обходить эту
защиту запрещено. Подключение — через официальный доступ (договор или
выгрузка открытых данных), отдельным решением владельца.

## Веб-поиск по номеру и имени

Сборщик `web_search` (`src/osint/collectors.ts`, запросы — `planning.ts`):

- номер — во всех записях (`+7…`, `8 (…)`, `8…`, `8-…`, `912 …`) и с
  `site:vk.com`, `site:ok.ru`, `site:t.me`;
- ФИО — в формах документа и подписи профиля, латиницей, с городом и
  местом работы из просьбы и по открытым страницам тех же соцсетей;
- почта — сама и имя ящика на страницах соцсетей;
- до 10 запросов и 6 прочитанных страниц на идентификатор.

Находка — страница или сниппет выдачи, где искомая строка есть дословно;
цитата берётся из того текста, где она найдена. Закрытые профили и то,
что поисковик не индексирует, недоступны — обходить это запрещено.
Город и место работы задаёт Ева (`city`, `workplace` у
`osint_investigate`), они хранятся в `osint_investigations.search_context`
и в граф не попадают.

## Итог исследования

Задание `osint_investigation` исполняет потребитель очереди `research`
(`src/jobs/consumer.ts`, по два задания одновременно). Когда исследование
завершено, Ева сама пересказывает отчёт: служебный ход в основном диалоге
человека под его блокировкой (`src/osint/narrator.ts`) с инструкцией, где
есть только id исследования; отчёт модель читает инструментом
`osint_get_report`. Пересказ уходит тем же путём, что её ответы, с ключом
доставки `osint-done:<id>`; повтор задания второго хода не запускает. Если
пересказ не удался, приходит шаблон со счётчиками без идентификаторов.

## Панель и тариф

**Панель → Настройки → блок «OSINT».** Все флаги переключаются без
перезапуска: значение панели главнее `.env`, удалённая настройка
возвращает значение окружения. При старте сервис переносит в панель значения
окружения, отличные от умолчания, если в панели их ещё нет: установка,
работавшая с `EVA_OSINT_ENABLED=true` до появления блока, не выглядит
выключенной, и сохранение других настроек её не выключает.

| Настройка | Ключ | По умолчанию |
|---|---|---|
| Исследования открытых источников | `runtime.osint_enabled` | выключено |
| ЕГРЮЛ/ЕГРИП ФНС | `runtime.osint_ru_registries` | выключено |
| Аккаунты по нику (Maigret) | `runtime.osint_collector_maigret` | включено |
| Веб-поиск упоминаний | `runtime.osint_collector_web` | включено |
| Домены и сети | `runtime.osint_collector_infrastructure` | включено |
| theHarvester | `runtime.osint_collector_theharvester` | включено |
| SpiderFoot | `runtime.osint_collector_spiderfoot` | включено |
| Исследований в сутки (предохранитель) | `runtime.osint_daily_limit` | 3 |

Как действует переключатель:
- инструменты `osint_*` попадают в набор хода, только пока контур включён;
  при переключении открытые сессии Letta закрываются (занятая — после
  хода), и следующий ход видит новый набор;
- `OsintService.create` проверяет флаг и лимиты при каждом вызове;
- обработчик задания зарегистрирован всегда: задание, поставленное при
  включённом контуре и выполняемое после выключения, закрывается как
  `cancelled` с причиной `osint_disabled`; выключенный источник в прогоне
  не участвует.

Контейнеры источников (`osint-worker`, `osint-harvester`,
`osint-spiderfoot`) панель не запускает: у неё нет доступа к Docker, и так
должно оставаться. Они включаются на сервере: `COMPOSE_PROFILES=osint`,
`OSINT_WORKER_TOKEN` в `.env`, затем `make update`. Источник, чей контейнер
не запущен, помечается в отчёте как не ответивший.

**Панель → Тарифы → метрика «OSINT-исследования» (`osint`).** Лимит — как у
поиска и документов: на сутки, неделю или месяц, `-1` — без ограничения,
`free_value` — на пробном периоде. Исследование списывает единицу при
создании (повтор с тем же ключом не списывает); при исчерпании
`osint_investigate` отвечает `osint_quota_exhausted`. Остаток виден в
`/balance`, Mini App и `get_subscription_status`. Начальные значения
(миграция 086): Free — 1 в месяц, Plus — 10, Max — 30.

## Порядок ввода

| Batch | Что входит |
|---|---|
| OSINT-1 (выполнен, #360) | доменный слой `src/osint/`, миграция 084, SQL-проверка границы арендатора, документ |
| OSINT-2 (выполнен, #362) | `osint-worker` (Python, внутренняя сеть): FtM, nomenklatura, Maigret, WhatsMyName, Sherlock как проверка; типизированный клиент |
| OSINT-3 (выполнен, #363) | репозиторий, оркестратор, итеративное расширение, сборщик SearXNG + Crawl4AI на адаптерах `ResearchOrchestrator`, задания, отмена, идемпотентность, хранение и удаление, флаги |
| OSINT-4 (выполнен, #364) | SpiderFoot (закрытый список пассивных модулей), theHarvester, RDAP, RIPEstat, DNS, CT |
| OSINT-5 (выполнен, #365) | инструменты Letta (`osint_investigate`, `osint_get_status`, `osint_get_report`, `osint_list`, `osint_search_entity`, `osint_cancel`, `osint_delete`), отчёт, уведомление, API Mini App, метрики, evals; `osint_expand` и `osint_explain_relationship` отложены |
| OSINT-6 (этот) | ЕГРЮЛ/ЕГРИП через публичный поиск ФНС за флагом `EVA_OSINT_RU_REGISTRIES`; «Прозрачный бизнес», ЕФРСБ и арбитраж не подключены (нет открытого машинного интерфейса) |
| далее | «Прозрачный бизнес», ЕФРСБ, арбитраж через официальный доступ; IntelOwl, Amass, GHunt (опционально), Aleph/OpenAleph, yente |

Флаги (переключаются и в панели, раздел «Панель и тариф»): `EVA_OSINT_ENABLED` (выключен), `EVA_OSINT_RU_REGISTRIES` (выключен), `EVA_OSINT_COLLECTOR_*`, `EVA_OSINT_DAILY_LIMIT`,
`EVA_OSINT_WORKER_URL`, `EVA_OSINT_HARVESTER_URL`, `EVA_OSINT_SPIDERFOOT_URL`;
osint-worker, osint-harvester и osint-spiderfoot включаются профилем compose
`osint`.
