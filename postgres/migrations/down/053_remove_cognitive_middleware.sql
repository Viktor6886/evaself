BEGIN;
-- Откат снимает только отметку версии.
--
-- Таблицы когнитивного слоя восстанавливают их собственные миграции
-- (012, 038, 041–048): полный откат идёт по всем down подряд и снова
-- вперёд по всем up, и в этом порядке схема собирается заново. Пустая
-- копия таблиц здесь означала бы, что откат «восстановил» память,
-- которой в ней нет.
DELETE FROM schema_migrations WHERE version='053_remove_cognitive_middleware';
COMMIT;
