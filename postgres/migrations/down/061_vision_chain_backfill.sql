-- Откат достройки цепочки зрения.
--
-- Строки не удаляются: к моменту отката цепочку мог поменять человек, и
-- отличить его выбор от достроенного нечем. Снимается только отметка о
-- миграции — повторный прогон снова ничего не сделает, потому что
-- цепочка уже непуста.
BEGIN;
DELETE FROM schema_migrations WHERE version = '061_vision_chain_backfill';
COMMIT;
