# Миграции, которые не выполняются на чистой установке.
#
# Каждая из них создаёт подсистему, удалённую миграцией 053: графовую и
# temporal-память, Memory Curator, Memory Doctor, Hybrid Retrieval,
# индекс навыков. На пустой базе создавать их, чтобы через сорок
# миграций удалить, незачем — это минуты установки и таблицы, которых
# никто не читает.
#
# Пропуск действует ТОЛЬКО на пустой базе. Существующая установка идёт
# всей цепочкой: её данные и её история никуда не деваются.
#
# Равенство обеих схем проверяется в CI (`scripts/ci/test-fresh-install.sh`).
# Миграцию, часть которой переживает удаление, сюда добавлять нельзя —
# проверка это покажет.
012_graph_memory.sql
042_temporal_memory.sql
043_memory_curator.sql
044_temporal_memory_indexes.sql
045_memory_doctor.sql
046_hybrid_retrieval.sql
047_skills.sql
