# Шаг 0. Аудит репозитория и фиксация baseline

- **Приоритет:** P0
- **Зависит от:** —
- **Feature flag:** нет
- **Объём:** 1 PR, без изменения поведения
- **Ветка:** `claude/step-00-bootstrap`

> Перед началом прочитай `CLAUDE.md` и `PROGRESS.md` в корне репозитория.
> Выполни только этот шаг, обнови `PROGRESS.md`, открой pull request и остановись.

---

Ты работаешь в репозитории Evaself. Это подготовительный шаг. Поведение системы менять нельзя.

ЦЕЛЬ:
Проверить, что инварианты в CLAUDE.md соответствуют фактическому репозиторию, зафиксировать фактическое состояние и создать воспроизводимую контрольную точку.

СНАЧАЛА ПРОВЕРЬ:
1. Фактический HEAD, ветку, наличие незакоммиченных изменений.
2. Реальный список сервисов в compose.yaml и ролей запуска.
3. Реальный список таблиц, представлений и расширений PostgreSQL из миграций.
4. Реальные версии зависимостей из package.json и lock-файлов.
5. Существующие тесты и то, какие из них проходят прямо сейчас.
Не доверяй никакому описанию репозитория, включая CLAUDE.md. Проверяй по коду.

СДЕЛАЙ:
1. Сверь CLAUDE.md с фактическим состоянием. Если какой-то инвариант не соответствует коду — не меняй код, а зафиксируй расхождение отдельным разделом в конце CLAUDE.md и в отчёте.
2. Создай docs/AUDIT_BASELINE.md: фактический список сервисов, таблиц, очередей, ролей процессов, feature flags, внешних интеграций и известных проблем, найденных в коде. Только факты с указанием файла и строки.
3. Создай непустую baseline-фикстуру: минимум два пользователя, разные conversations, профиль, цели, задачи, записи inbox и outbox, квота или подписка, один побочный эффект инструмента и одно ожидающее напоминание.
4. Выполни существующий backup и restore на чистом тестовом окружении.
5. Зафиксируй baseline-замеры: время сборки, прохождение тестов, задержки, размер пулов, поведение сессий Letta.

НЕ ДЕЛАЙ В ЭТОМ ШАГЕ:
Не меняй код приложения, миграции, схему, compose и зависимости. Не создавай feature flags. Не исправляй найденные проблемы — только зафиксируй их.

ТЕСТЫ:
Запусти существующие наборы полностью и зафиксируй фактический результат, включая падающие тесты.

ГОТОВО, КОГДА:
docs/AUDIT_BASELINE.md существует, расхождения с CLAUDE.md зафиксированы, baseline-фикстура воспроизводится, backup восстанавливается на чистом окружении, поведение системы не изменилось.

ОТЧЁТ:
По формату из CLAUDE.md. Обязательно приложи реальный вывод команд.
